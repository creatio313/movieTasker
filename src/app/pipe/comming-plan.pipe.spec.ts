import { CommingPlanPipe } from './comming-plan.pipe';

describe('CommingPlanPipe', () => {
  it('create an instance', () => {
    const pipe = new CommingPlanPipe();
    expect(pipe).toBeTruthy();
  });
});
